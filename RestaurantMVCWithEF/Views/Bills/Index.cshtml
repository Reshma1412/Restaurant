@model IEnumerable<RestaurantMVCWithEF.Models.Bill>

@{
    ViewBag.Title = "Index";
}

<h2>Bills Information</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BillAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.CustomerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderInfo.OrderID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RestaurantDetail.RestuarantName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BillAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.CustomerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderInfo.OrderID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RestaurantDetail.RestuarantName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BillsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BillsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BillsID })
        </td>
    </tr>
}

</table>
